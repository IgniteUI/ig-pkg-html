{
	"overview": null,
	"name": "Container",
	"category": null,
	"visual": true,
	"events": {
		"click": {
			"cancellable": true,
			"args": [
				{
					"name": "e",
					"type": "event",
					"optional": true,
					"description": "Reference to event object."
				}
			],
			"description": "Fires on a mouse click on the element."
		},
		"dblclick": {
			"cancellable": true,
			"args": [
				{
					"name": "e",
					"type": "event",
					"optional": true,
					"description": "Reference to event object."
				}
			],
			"description": "Fires on a mouse double-click on the element."
		},
		"mousedown": {
			"cancellable": true,
			"args": [
				{
					"name": "e",
					"type": "event",
					"optional": true,
					"description": "Reference to event object."
				}
			],
			"description": "Fires when a mouse button is pressed down on an element."
		},
		"mouseout": {
			"cancellable": true,
			"args": [
				{
					"name": "e",
					"type": "event",
					"optional": true,
					"description": "Reference to event object."
				}
			],
			"description": "Fires when the mouse pointer moves out of an element."
		},
		"mouseover": {
			"cancellable": true,
			"args": [
				{
					"name": "e",
					"type": "event",
					"optional": true,
					"description": "Reference to event object."
				}
			],
			"description": "Fires when the mouse pointer moves over an element."
		},
		"mouseup": {
			"cancellable": true,
			"args": [
				{
					"name": "e",
					"type": "event",
					"optional": true,
					"description": "Reference to event object."
				}
			],
			"description": "Fires when a mouse button is released over an element."
		}
	},
	"properties": {
		"tagName": {
			"type": "string",
			"defaultValue": "div",
			"changeTag": true,
			"valueOptions": [
				{
					"name": "div",
					"type": "string",
					"description": "Initialize HTML DIV."
				},
				{
					"name": "section",
					"type": "string",
					"description": "Initialize HTML Section."
				},
				{
					"name": "nav",
					"type": "string",
					"description": "Initialize HTML NAV."
				},
				{
					"name": "article",
					"type": "string",
					"description": "Initialize HTML Article."
				},
				{
					"name": "aside",
					"type": "string",
					"description": "Initialize HTML Aside."
				},
				{
					"name": "header",
					"type": "string",
					"description": "Initialize HTML Header."
				},
				{
					"name": "footer",
					"type": "string",
					"description": "Initialize HTML Footer."
				},
				{
					"name": "address",
					"type": "string",
					"description": "Initialize HTML Address."
				},
				{
					"name": "main",
					"type": "string",
					"description": "Initialize HTML H6."
				}
			]
		},
		"innerHTML": {
			"type": "string",
			"defaultValue": "",
			"description" : "The display text."
		},
		"style": {
			"type": "string",
			"defaultValue": null,
			"description": "Specifies an inline CSS style for an element."
		},
		"title": {
			"type": "string",
			"defaultValue": null,
			"description": "Specifies extra information about an element."
		}
	}
}